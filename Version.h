/*
 * File generated by VerUpdate v0.1
 * 
 */

namespace Version {

struct Info {
	unsigned Major;
	unsigned Minor;
	unsigned Build;
	char* BuildDate;

	std::string VersionString() const {
		char buf[128];
		sprintf(buf, "v%d.%d build %d", Major, Minor, Build);
		return std::string(buf);
	}
	std::string VersionStringFull() const {
		char buf[128];
		sprintf(buf, "v%d.%d build %d on %s", Major, Minor, Build, BuildDate);
		return std::string(buf);
	}

};

} //namespace Version

