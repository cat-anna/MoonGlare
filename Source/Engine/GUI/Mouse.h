/*
  * Generated by cppsrc.sh
  * On 2015-03-01  7:44:40,30
  * by Paweu
*/
/*--END OF HEADER BLOCK--*/

#pragma once
#ifndef Mouse_H
#define Mouse_H

namespace MoonGlare {
namespace GUI {

class Mouse : public cRootClass {
	GABI_DECLARE_STATIC_CLASS(Mouse, cRootClass);
	DISABLE_COPY();
public:
 	Mouse(GUIEngine *Owner);
 	virtual ~Mouse();

	bool Initialize();
	bool Finalize();

	void Process(const Core::MoveConfig &conf);
	void Draw(Graphic::MatrixStack &dev);
	static void RegisterScriptApi(ApiInitializer &api);

	void SetHovered(iWidget *Widget);

	void SetCursorImage(const string& Name);

	void OnMouseDown(Events::MouseDownEvent &event);
	void OnMouseUp(Events::MouseUpEvent &event);
protected:
	Point m_Position;
	Rect m_PositionLimit;
	GUIEngine *m_Owner;
	Widgets::Image *m_Image = nullptr;
	Screen *m_Screen = nullptr;

	iWidget *m_HoveredWidget = nullptr;
};

} //namespace GUI 
} //namespace MoonGlare 

#endif
